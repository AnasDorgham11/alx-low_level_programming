#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 * description - a to z
 * 
 * Return: Always 0 (Success)
 */

int main(void)
{
	for (int i= 'a'; i<= 'z'; i++)
    {
        putchar(i);
    }

    return (0);
}
